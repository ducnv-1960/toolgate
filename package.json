{
  "name": "toolgate",
  "version": "1.0.0",
  "description": "Toolgate — aggregates multiple MCP servers, provides semantic tool search and unified execution",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "setup": "npm install && cd dashboard && npm install && npm run build && cd ..",
    "dev": "tsx watch src/main.ts",
    "build": "tsc && npm run build:dashboard",
    "build:dashboard": "cd dashboard && npm run build",
    "start": "node dist/main.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@huggingface/transformers": "^3.5.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "better-sqlite3": "^11.10.0",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "vectra": "^0.9.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.2",
    "@types/node": "^22.15.18",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
